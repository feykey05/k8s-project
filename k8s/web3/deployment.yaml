apiVersion: apps/v1
kind: Deployment
metadata:
  name: web3-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web3
  template:
    metadata:
      labels:
        app: web3
    spec:
      containers:
      - name: web3
        image: nginx:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
      volumes:
      - name: html
        configMap:
          name: web3-html
---
apiVersion: v1
kind: Service
metadata:
  name: web3-service
spec:
  type: ClusterIP
  selector:
    app: web3
  ports:
  - port: 80
    targetPort: 80
